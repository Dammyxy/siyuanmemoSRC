# 测试优化说明

## 问题诊断

测试运行缓慢或卡死的主要原因：

1. **属性测试（Property-based tests）运行次数过多**
   - fast-check 默认运行 100 次
   - 多个属性测试累积导致总运行时间过长

2. **测试并发配置不当**
   - 默认配置可能导致资源竞争
   - 某些测试可能相互阻塞

3. **缺少超时配置**
   - 测试卡死时没有自动终止机制

## 优化方案

### 1. 配置 fast-check 运行次数

在 `vitest.setup.ts` 中配置：
- 本地开发：10 次运行（快速反馈）
- CI 环境：100 次运行（完整验证）
- 超时时间：3 秒

### 2. 优化 Vitest 配置

在 `vitest.config.ts` 中：
- 设置测试超时：5 秒
- 使用线程池并发
- 最多 4 个线程

### 3. 分离测试命令

```bash
# 快速测试（跳过属性测试）
npm run test:quick

# 只运行属性测试
npm run test:property

# 完整测试
npm run test:run

# 交互式测试
npm run test:ui
```

## 使用建议

### 日常开发
```bash
npm run test:quick
```
- 跳过耗时的属性测试
- 快速验证核心功能
- 适合频繁运行

### 提交前验证
```bash
npm run test:run
```
- 运行所有测试
- 包括属性测试（减少运行次数）
- 确保代码质量

### CI/CD
```bash
CI=true npm run test:run
```
- 完整的属性测试（100 次运行）
- 全面的代码覆盖率
- 严格的质量保证

## 性能对比

| 命令 | 预计时间 | 测试覆盖 |
|------|---------|---------|
| test:quick | ~10-20s | 核心功能 |
| test:run | ~30-60s | 全部（本地） |
| CI=true test:run | ~2-5min | 全部（CI） |

## 故障排查

### 测试仍然卡死？

1. 检查是否有未处理的 Promise
2. 查看是否有无限循环
3. 使用 `--reporter=verbose` 查看详细输出
4. 单独运行问题测试文件：
   ```bash
   npx vitest run path/to/test.ts
   ```

### 属性测试失败？

1. 查看失败的输入值
2. 使用 `fc.sample()` 生成示例数据
3. 调整生成器的约束条件
4. 增加 `numRuns` 以发现更多边界情况

## 最佳实践

1. **优先编写单元测试**
   - 快速、可靠
   - 易于调试

2. **谨慎使用属性测试**
   - 用于验证通用属性
   - 不要过度使用

3. **设置合理的超时**
   - 单个测试：5 秒
   - 属性测试：3 秒/次

4. **使用并发测试**
   - 独立的测试可以并行
   - 注意共享状态

5. **定期清理测试**
   - 删除重复测试
   - 合并相似测试
   - 优化慢速测试
