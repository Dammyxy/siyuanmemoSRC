# Full History Resume Audit Report

审计日期：2026-04-29

## 1. 审计窗口

本轮按 `docs/FULL_HISTORY_RESUME_AUDIT_PROMPT.md` 的 Prompt A 执行，只审计，不修生产代码。

- 已审：`docs/DDD_RESCAN_BACKLOG.md` 当前可枚举的全部真实 task delta；`Entry template` / `YYYY-MM-DD - <short task name>` 模板 heading 不计入 task delta。
- 已审范围一：从 `2026-04-29 - same-day elapsedDays dirty repair` 到 `2026-04-27 - manual queue forced formal review semantics`。
- 已审范围二：从 `2026-04-27 - retrieval explicit manual-add preservation` 到 `2026-04-24 - reveal CTA height recovery, direct-child drag hit zones, and browser neutralization cleanup`。
- 已审范围三：从 `2026-04-24 - review native density rollback, drag hit repair, and browser highlight reassignment` 到 `2026-04-22 - filter-group topic next hides current scope item`。
- 已审范围四：从 `2026-04-22 - native riff remove routing and managed-local delete convergence` 到 `2026-04-16 - AI workbench tree chat experience vNext`。
- 已审范围五：从 `2026-04-16 - AI declarative user skills` 到 `2026-04-15 - ai structured json-mode hardening`。
- 已审范围六：从 `2026-04-15 - ai workbench chat sessions and persistent history` 到 `2026-03-11 - browser neural jump handoff to existing review tab`。
- 已审范围七：从 `2026-03-11 - wake convergence render split and lazy detail loading` 到文件尾部补录的 `2026-04-21 - review current-item hydration unification`；其间 `Entry template` / `YYYY-MM-DD - <short task name>` 仅为模板，不作为任务结论。
- 未审：无真实 task delta 未审；文件尾部 Round33 summary 作为债务提示扫过，但不按 task delta 计数。
- 审计基线：active runtime 代码优先，重点核对 `src/application/ApplicationContext.ts`、`src/types/unified-data-source.ts`、SQL persistence、review commit、queue domain、Browser query、Arena、AI workbench、review UI 主链。
- 总结：P0 0 个，P1 0 个，P2 0 个，P3 51 组。

## 2. 高风险发现表

| ID | 风险 | 条目 | 声称完成 | 实际证据 | 缺口 | 影响路径 | 建议修复批次 |
|---|---|---|---|---|---|---|---|
| 无 | - | 当前无开放 P0/P1/P2 | A-01/A-12 均已按 active runtime 证据闭环或转入安全历史锚点 | 旧 backlog wording 的误导风险已在安全表标注 | 无 active runtime 缺口 | 无 | 下一步转 P3 性能/架构债 |

## 3. 误报 / 已确认安全表

| 条目组 | 结论 | 代码证据 | 测试 / grep 证据 |
|---|---|---|---|
| SQL algorithm state authority | 安全，未见 active runtime 缺实现 | `deriveAlgorithmCardState()`、`applyAlgorithmCardState()`、`getAlgorithmCardStateDiagnostic()`、`backfillAlgorithmCardStates()`、`migrateAlgorithmCardStateIfNeeded()` 都在 active SQL path | `algorithmCardState.test.ts`、`SqlUnifiedStorageRepository.test.ts`、`SqliteMigrationService.test.ts`、`fsrsReviewStateRepair.test.ts` |
| SQL algorithm state real-db closure | 安全，A-01 已关闭：真实库只读精确诊断 clean，未写原库 | `diagnostics:algorithm-state` 读取 `siyuanmemo.db` bytes 到 sql.js 内存库，镜像 active `algorithm_card_state` diagnostic/backfill simulation；真实库 `total=426`、`dirty=0`、`missingStateRows=0`、`invalidStateRows=0`、`cardStateMismatches=0`、`orphanStateRows=0`、`afterDirty=0` | `pnpm diagnostics:algorithm-state -- --db "H:\SiYuanXY\data\storage\petal\siyuan-plugin-siyuanmemo\siyuanmemo.db"` |
| Manual queue historical semantics closure (A-02/A-04) | 安全，旧 forced formal review / `sessionManualAddIds` wording 已被当前 manual-early-review 语义覆盖 | `RetrievalPracticeQueue` / `IncrementalLearningQueue` 通过 `getFilteredReviewDefault()` 区分默认 `preview-only` 和显式 `reschedule`；`sessionManualAddIds` / `pruneCompletedManualCards` 已非 active `src/` 符号 | `UnifiedQueueStrategy.performance.test.ts`、`DynamicQueue.review-removal.test.ts`、`ARCHITECTURE.md` preview-only 说明；`rg "sessionManualAddIds|pruneCompletedManualCards" src` 无命中 |
| SQL/Arena foundation evidence closure (A-03) | 安全，旧条原始 Validation 只有 build 的证据债，已被后续 SQL/Arena/ReviewCommit focused tests 追平 | `SqliteDatabaseService` binary persist / restore、SQL repositories、Arena SQL batch、`ApplicationContext` SQL wiring 均在 active path | sqlite repository/migration tests、`ReviewCommitUseCase.test.ts`、`ArenaKernelService.test.ts`、`SrsTransparencyApplicationService.test.ts` |
| UI/source history closure (A-06/A-07/A-08/A-09) | 安全，旧视觉命名、toolbar、missing-block、source-refresh wording 只作演进史 | `Arena Challenger` labels、current `ReviewActions` / `ReviewHeader` / `SRSBrowser.scss`、hidden missing-block UI with legacy SQL query compat、`reviewSourceBlockRefreshEnabled=false` default-off | `srsDisplayLabels` / Arena tests、Review/Browser toolbar specs、missing-block UI tests、source refresh tests |
| AI/progressive historical contract closure (A-10/A-11/A-12) | 安全，旧 explain-only / promptProfiles / Alt+X 合同已被 declarative skills、concept-coach、`⌥⇧X` routing 覆盖 | `AIChatToolExecutorService`、`AIChatSkillRegistry`、`AIWorkbenchLegacyView` compat-only、session store drops `candidate-board`、`PROGRESSIVE_EXCERPT_REQUEST_EVENT` | AI workbench/session/settings tests、progressive hotkey/review route tests |
| Browser SQL read model / source existence | 安全，SQL page/count/source cache 有 fail-open fallback，不返回错页 | `BrowserDeckReadPort`、`SqlUnifiedStorageRepository.queryDeckPage/queryDeckMatchedIds/countCards/getBrowserStats`、`SourceExistenceCache`、`BrowserApplicationService.tryReadSqlDeckPage()` | `BrowserApplicationService.deck-query.test.ts`、`QueueBrowserQueryKernel.test.ts`、`BrowserDeckQueryKernel.scope-doc-ids.test.ts`、`SqlUnifiedStorageRepository.test.ts` |
| Browser SQL D-01 profile closure | 安全，D-01 已关闭：真实库与 1k/5k 内存扩容 profile 全部过预算，原库只读未写入 | `diagnostics:browser-sql-profile` 读取真实 `siyuanmemo.db` bytes 到 sql.js 内存库，按 Browser SQL read-port projection/source-existence query 形状跑 stats/page/matched ids/LIKE/source summary/source update simulation；真实库 `rowCount=426`，扩容目标 `1000/5000`；5k 最大耗时：stats `7.196ms`、page `11.379ms`、matched ids `13.938ms`、LIKE `15.187ms`、source summary `5.515ms`、source update simulation `94.608ms` | `pnpm diagnostics:browser-sql-profile -- --db "H:\SiYuanXY\data\storage\petal\siyuan-plugin-siyuanmemo\siyuanmemo.db"`；focused Browser SQL/source/batch persist tests 22 tests 通过 |
| Review feedback transaction / rollback | 安全，普通失败和 stale item 分开处理 | `ReviewCommitUseCase.execute()` 包 `review.feedback` transaction；`UnifiedQueueStrategy` 对 `QueueItemUnavailableError` 和普通错误分流，普通错误恢复 queue snapshot/current item/card snapshot | `ReviewCommitUseCase.test.ts`、`UnifiedQueueStrategy.performance.test.ts`、`useReviewSession.spec.ts` |
| Review breadcrumb port cleanup | 安全，production renderer 已走 `ReviewSiyuanPort`，未见 review production direct infra import 回流 | `loadBreadcrumbTrail.ts` 接收 `ReviewSiyuanPort.getBlockBreadcrumb`；`XiuyuanListTemplateCard.vue`、`ImageOcclusionCardRenderer.vue` 通过 prop port 取 breadcrumb | `loadBreadcrumbTrail.test.ts`、`XiuyuanListTemplateCard.test.ts`、`ImageOcclusionCardRenderer.test.ts`、`rg \"@/infrastructure/siyuan/api\" src/ui/review` 仅命中 test mock / 非本轮 render path |
| SRS Arena context alignment | 安全，评分后才产生 recommendation，并带 rating/context | `ArenaKernelService` 接收 `ratingBasis` 和 `SrsV2SchedulingContext`；`ReviewCommitUseCase` 传 `command.context`；`ReviewView` 打开 `SrsArenaConflictDialog` 时传当前 rating/context | `ArenaKernelService.test.ts`、`SrsTransparencyApplicationService.test.ts`、`ReviewView.srs-editor-schedule.spec.ts`、`SrsArenaConflictDialog.spec.ts` |
| Neural roam `fsrs_cards LIMIT` | 安全，旧 SQL 失败已 fail-open 且缓存不可用状态 | `ConceptQueryEngine` 先用 injected node resolver；`SiyuanNeuralRoamCardTypeResolverAdapter` 查询不含 `LIMIT`，缺表/语法错后跳过重复 SQL | `ConceptQueryEngine.isConceptCard.test.ts`、`SiyuanNeuralRoamCardTypeResolverAdapter.test.ts` |
| Neural roam AI virtual context closure | 安全，A-05 已关闭：整份 review-session AI 测试现在通过，不再只靠 `-t "neural roam virtual"` | `AIReviewNeuralContext`、`sourceVirtualNodeId`、`AIWorkbenchService` 虚拟节点内容注入、`GetCurrentContext` 工具链仍在 active path | `pnpm exec vitest run src/application/services/__tests__/AIWorkbenchService.review-session.test.ts --reporter=dot`，34 tests 通过 |
| Review source refresh throttling | 安全，默认关闭，开启后共享 transaction service | `settings.ui.reviewSourceBlockRefreshEnabled` 默认 false；`ApplicationContext.updateTransactionWebSocketService()` 统一启停；`ReviewView` register/unregister handler | `ReviewView.source-block-refresh.spec.ts`、`TransactionWebSocketService.test.ts`、`SettingsPanel.test.ts` |
| Runtime-only visible review AI context sync | 安全，隐藏 AI 不再触发持久化刷新 | `ReviewView.syncReviewAIContextIfNeeded()` 先判断 sidecar/companion 可见；`ReviewAIWorkbenchRegistry.updateReviewSessionContext()` 对 hydrated service 调 `updateLiveReviewContext()` | `ReviewAIWorkbenchRegistry.test.ts`、`ReviewView.more-menu.spec.ts`、`AIWorkbenchService.review-session.test.ts` |
| CDF concept-card reuse | 安全，幂等策略在 active CDF use case / resolver 路径 | `CreateCdfMultilineCardsUseCase` 与 `ConceptCardResolver` 使用 `duplicatePolicy: 'reuse-existing'` | `CreateCdfMultilineCardsUseCase.test.ts`、`ConceptCardResolver.test.ts` |
| Prepared review transition / Protyle flicker | 安全，prepared 失败是 UI soft failure，普通 Protyle 有 pending host | `reviewSessionController` 保持当前卡；`reviewPresentationPreparer` 路由 inline formula multi-cloze；`ReviewContent` active Protyle 等 pending ready 后再替换 | `useReviewSession.spec.ts`、`reviewPresentationPreparer.test.ts`、`ReviewContent.editor-state.spec.ts`、special renderer tests |
| FSRS drift / zero-stability repair | 安全，active storage/scheduler/load path 仍有共享 repair | `fsrsReviewStateRepair.ts`、`normalizeSchedulerCard.ts`、`TSFSRSScheduler`、`UnifiedStorageManager`、`SqlUnifiedStorageRepository` 都保留修复/水合入口 | scheduler repair/router、storage migration、Riff mapper、queue snapshot cache tests |
| Browser / Review boundary convergence | 安全，核心倒挂已切断；剩余为 P3 架构债 | `BrowserApplicationService` 和 browser query shared 层承接 DTO/query，Review render services 由 application factory 注入，旧 Review controller/provider 路径已移除 | `pnpm check:boundaries` 为该条 validation；`scripts/check-boundaries.cjs` 和 focused Browser/Review tests 存在 |
| Browser exact subset / SQL real-card results | 安全，浏览器选中卡复习和 SQL 查询都走真实 cardId | `DeckDataSource` 传 `cardIds`，`DialogManager` 创建 `SubsetReviewQueue` 时保留 exact ids，`OrderedStaticSubsetQueueBase` 不扩同块 sibling；`QueryDataSource` 用 projection/manager hydrate 真实 `fsrsCardId` | `SubsetReviewQueue.preferred-card.test.ts`、`DialogManager.review-header-variant.test.ts`、`DeckDataSource.set-priority.test.ts`、`QueryDataSource.queryable.test.ts` |
| Arena default-off / v1 unified kernel | 安全，实验 Arena 不再默认影响复习主线 | `DEFAULT_ARENA_SETTINGS.enabled=false`，`normalizeArenaSettings()` 迁移旧 enabled，`ArenaKernelService.isEnabled()` 早退，`DialogManager` / `MenuManager` 隐藏入口 | `settings-normalization.test.ts`、`ArenaKernelService.test.ts`、`SrsTransparencyApplicationService.test.ts` |
| Queue session loop guard / stale card recovery | 安全，Good/Easy 本轮排除和 stale item 分流存在 | `UnifiedQueueStrategy.sessionExcludedCardIds`、`requiresFullRefresh`、`card-deleted` local cleanup、`restoreCardSnapshotForFailedFeedback`、`reviewSessionController` stale handling | `UnifiedQueueStrategy.performance.test.ts`、`UnifiedQueueStrategy.hide-current-in-scope.test.ts`、`useReviewSession.spec.ts`、`ReviewScopeCardCreationSyncService.test.ts` |
| Storage tombstone / delete sync / basic symbol hardening | 安全，误制卡、已删实体回魂、取消闪卡回魂三条 active path 有代码闭环 | `BasicDirectionRule` 走共享 parser；`UnifiedStorageManager` / SQL repo 持久 tombstones；`CardDeletedEvent` / `CardsDeletedEvent` 携带 blockId(s)；`RiffSyncEventHandler` 统一 delete sync | `BasicDirectionRule.test.ts`、`UnifiedStorageManager.sync-conflict.test.ts`、delete use case tests、`XiuyuanSyncService.native-riff-semantic-routing.test.ts` |
| Shared CDF direct review rendering | 安全，Review active semantic cards 已走共享 scene/renderKind | `CdfDirectScene`、`renderReviewMarkdown`、`CdfDirectLayout`、concept-definition/descriptor/list-template renderer 共享 `fragment` / `block-flow` contract；semantic fallback 有日志 | CDF render service tests、renderer specs、`ReviewContent.editor-state.spec.ts`、`ReviewContent.fonts.spec.ts` |
| Review native editor / dialog titlebar / reveal UI | 安全，current dialog handle、原生 titlebar host、Escape exit、soft refresh 都有 active code | `registerCurrentReviewDialog()` identity-safe；`ReviewView` titlebar host/rebind；`ReviewContent.exitEditorByEscape()` / `refreshVisibleContent()`；`renderEpoch` soft refresh；`SkipMenuButton` native menu | `DialogManager.review-header-variant.test.ts`、`ReviewView.queue-switch.spec.ts`、`ReviewHeader.spec.ts`、`ReviewActions.spec.ts`、`reviewDialogEscape.test.ts`、`ReviewContent.editor-state.spec.ts` |
| UI shell / settings / dialog visual skin | 安全，F toolbox 视觉改造已是外围 shell/token 层，不是当前 runtime correctness 声明 | `siyuanmemo-admin-skin.scss`、`createVueDialog()` variants、AI settings dialogs、SettingsPanel 管理器入口仍在 active path | dialog chrome/settings/AI dialog/Browser/Review focused specs |
| AI settings manager and compact workbench | 安全，工具审批、内置 Prompt、用户 Skill 管理器和空态 CTA 都有 active component/test | `AiToolPermissionManagerDialog.vue`、`AiBuiltInPromptEditorDialog.vue`、`AiUserSkillEditorDialog.vue`、`AiSettingsDraggableList.vue`、`AiWorkbenchPane.vue` | settings AI dialog specs、`AIChatToolExecutorService.test.ts`、`AiWorkbenchPane.compact-surface.spec.ts` |
| Custom tab early bootstrap | 安全，插件启动先注册 custom tabs，再等 `contextReady` lazy mount | `index.ts` 有 `customTabsRegistered` / `contextReady` / `bootstrapDeferredCustomTab()`；`TabManager` 有 `initBrowserTab` / `initReviewTab` / `initReviewAICompanionTab` | `index.test.ts` custom tab bootstrap、TabManager review transfer/AI/open window specs |
| Progressive Topic/Item selection chain | 安全，Topic continuation、manual cloze、current-block batch fill、derived item render policy 都在 active path | `SelectionTopicContinuationService`、`TopicDerivedItemService`、`SelectionClozeMarker`、`ProgressiveExcerptHotkeyHandler`、`BlockMenuHandler`、`reviewRenderPolicy` | Selection/Topic/Progressive/Review focused tests |
| SiYuan 3.6.5 block mutation normalization | 安全，`updateBlock()` 在 infrastructure 边界归一化缺失 op id 的响应 | `src/infrastructure/siyuan/api.ts` 归一 `doOperations`，缺 id 时回退请求 block id；Progressive/Review/AI adapter 共享此 API | `api.block-mutation.test.ts`、ProgressiveExcerptHotkeyHandler/SelectionTopicContinuation tests |
| Native cloze and CDF prompt routing | 安全，普通 cloze 回主 Protyle，公式 cloze 保留 special renderer；concept-coach CDF 仍是 `items[].text` cue-arrow 合同 | `RenderProfileResolver`、`UnifiedReviewAdapter`、`reviewRenderPolicy`、`AIPromptContractRegistry`、`types/settings.ts`；`CreateDescriptorCards` 工具的 `cue/answer` 是另一条 explicit write-tool 合同 | `ReviewContent.render-profile-routing.test.ts`、`AIPromptContractRegistry.test.ts`、`AIPromptComposer.test.ts`、`AIFlashcardToolService.test.ts` |
| Browser delete route / card CRUD handoff | 安全，browser datasource 已走 `UnifiedDataSourceManager.deleteCard/batchDeleteCards`，不是 UI 直连 CardApplicationService | `DataSourceUtils.deleteBrowserCards()`、`DeckDataSource`、`QueryDataSource`、`UnifiedDataSourceManager.deleteCard/batchDeleteCards` | `DataSourceUtils.delete-browser-cards.test.ts`、browser datasource tests |
| Doc-tree browser scope and filter-group hide current | 安全，`scopeDocIds` 与 session hide-current 都在 active code | `BrowserOpenState.scopeDocIds`、`DeckDataSource` / queue datasource filters、`CoreReviewEntryService` forwarding、`UnifiedQueueStrategy.hideCurrentInScope` path | `BrowserDeckQueryKernel.scope-doc-ids.test.ts`、`DialogManager.browser-tab-convert.spec.ts`、`UnifiedQueueStrategy.hide-current-in-scope.test.ts` |
| Native Riff remove / managed-local delete | 安全，native cancel 已走 `ws-main -> NativeRiffSyncTriggerHandler -> XiuyuanSyncService.handleNativeRiffRemove()`，只删 `riff-managed` 本地 Xiuyuan | `extractNativeRiffRemoveBlockIds()`、`pendingNativeRemoveBlockIds`、`buildNativeRiffRemoveChangeSet()`、`isManagedRiffXiuyuan()`、`applyPlannedSync()` | `NativeRiffSyncTriggerHandler.test.ts`、`XiuyuanSyncService.malformed-riff-input.test.ts` 覆盖 direct remove、in-flight queue、local-owned no-op |
| CDF live cue-answer / semantic CDF | 安全，review live descriptor 与 AI semantic CDF 当前主链都有 fallback | `parseCueAndAnswer()`、`DescriptorCardRepository` live group/cue/answer lookup、`AIFlashcardToolService.buildSemanticCdfScanResult()` rows/kramdown fallback、`CreateCdfMultilineCardsUseCase.executeFromScanResult()` | `DescriptorCardRepository.cdf-fusion.test.ts`、`DescriptorCardRenderService.cdf-fusion.test.ts`、`AIFlashcardToolService.test.ts` |
| Debug log runtime setting | 安全，设置不再是 dead config | `SettingsPanel.vue` 暴露 `ui.enableDebugLogs`；`SettingsService.init/updateSettings()` 调 `applyRuntimeLogSettings()`；`logger.applyDebugLogPreference()` 安装 console bridge 并切 `debug/warn` | `SettingsService.logging.test.ts`、`SettingsPanel.test.ts` |
| AI provider endpoint / empty-body fallback | 安全，relative endpoint 和 DeepSeek empty body 都在 transport 边界处理 | `OpenAICompatibleLLMAdapter.resolveEndpointUrl()` 拼 `baseUrl`；`resolveStructuredRequestProfile()` 对 DeepSeek/unstable json_object 走 `json_object -> json_object -> prompt_json`；diagnostic 带 endpoint/model/profile | `OpenAICompatibleLLMAdapter.test.ts` 覆盖 relative OpenAI/Gemini endpoint、empty content parse、DeepSeek empty-body retry、prompt_json fallback |
| AI self-test native card creation | 安全，旧 plugin modes 已收缩，active creation 经 `AISelfTestCardCreationService` 调 native list/mark/heading/super-block tools | `AISelfTestCardCreationService.createFromCandidates()`、`AISelfTestDraftSupport`、`AIFlashcardToolService.resolveNativeRiffStructureWithFallback()`、self-test target memory | `AISelfTestCardCreationService.test.ts`、`AIWorkbenchService.review-session.test.ts`、`AiWorkbenchPane.compact-surface.spec.ts` |
| Review AI queue-shared chat / tree session | 安全，review chat 以 `reviewChatKey` 复用队列会话，tree schema 会迁移旧 thread | `ReviewAIWorkbenchRegistry.updateReviewSessionContext()`、`AIWorkbenchSessionStoreService.reviewChatKey`、`migrateThreadsToTree()`、`activeLeafNodeIds`、`ReviewView.buildReviewAIChatKey()` | `ReviewAIWorkbenchRegistry.test.ts`、`AIWorkbenchSessionStoreService.test.ts`、`AIWorkbenchService.review-session.test.ts` |
| Queue runtime spec / incremental requery | 安全，incremental feedback 后 requery 和 visible identity avoidance 在 active strategy | `UnifiedQueueStrategy.avoidOnceCardId/avoidOnceBlockId`、`selectNextRequeryCardIndex()`、legacy `deferOnceCardId` snapshot compat、`FilterGroupQueue.restoreSessionSnapshot()` | `UnifiedQueueStrategy.performance.test.ts`、`FilterGroupQueue.session-transfer.spec.ts`、`review-tab.ts` compat fields |
| Xiuyuan two-phase sync / ownership authority | 安全，旧 queue 条目 deferred 的 sync P1/P2 已由后续 2026-04-17 条目偿还 | `SyncChangeSet`、`XiuyuanRepository.applySyncChangeSet()`、`runWriteTransaction()`、rollback snapshot、`chooseCanonicalXiuyuan()`、`meta.ownership` | `logicalKeys.ownership.test.ts`、`XiuyuanRepository.sync-change-set.test.ts`、`XiuyuanSyncService.malformed-riff-input.test.ts` |
| AI declarative skills / universal chat shell | 安全，active AI runtime 已统一成 built-in + `user:<id>` skill registry，未见双 runtime | `AIChatSkillRegistry` 读取 `settings.ai.userSkills[]`；`AIWorkbenchSkillRegistry`、`AIWorkbenchService`、`AIWorkbenchSessionStoreService` 保留 generic structured result/thread | `AIChatSkillRegistry.test.ts`、`AIWorkbenchService.user-skill.test.ts`、`AIWorkbenchSessionStoreService.test.ts`、`settings-normalization.test.ts` |
| AI structured response / provider diagnostics | 安全，早期 `空正文` / alias / raw fallback 已并入 current adapter/service | `OpenAICompatibleLLMAdapter` 处理 object content、`message.parsed`、`choice.text`、DeepSeek retry/profile diagnostics；`AIWorkbenchService` 归一 legacy alias/rawContent | `OpenAICompatibleLLMAdapter.test.ts`、`AIWorkbenchService.review-session.test.ts`、`AiWorkbenchPane.compact-surface.spec.ts` |
| Review current-card refresh race | 安全，local advance 后 stale refresh 被 expected identity guard 拦下 | `reviewSessionController.refreshCurrentItem()` 接 `expectedCurrentCardId/expectedCurrentBlockId`；`ReviewView.buildExpectedRefreshOptions()` 在异步 refresh 调用处使用 | `useReviewSession.spec.ts`、`ReviewView.local-advance-race.spec.ts` |
| Review tab split runtime snapshot | 安全，special renderer split/reload 有 `reviewState` runtime snapshot 和 surface refresh | `TabManager` 读写 `runtime.data.reviewState`；`ReviewView.buildReviewTabRuntimeState()`；`UnifiedQueueStrategy.restoreSessionSnapshot()`；`refreshTabSurface()` | `TabManager.review-transfer.spec.ts`、`ReviewView.open-as-menu.spec.ts`、`ReviewView.neural-tab-bridge.spec.ts` |
| Review special-render split/source identity | 安全，native split guard 与 locate-source source/card identity 分开 | `ReviewView` 阻断 active special renderer native split；`resolveCurrentReviewSourceBlockId()` / editable source path 分离 locate-source；`ReviewSiyuanPort` 承接 edit/read | `ReviewView.native-split-guard.spec.ts`、`ReviewView.open-as-menu.spec.ts`、`ReviewContent.editor-state.spec.ts` |
| Riff reconciliation / AutoCard idempotency | 安全，active path 已有 local-owned 优先、riff-managed 标记、create-card logical reuse | `XiuyuanSyncService` 跳过同 block local-owned；`XiuyuanRepository` 不给 managed riff 写 `custom-xiuyuan-id`；`CreateCardUseCase` 复用 existing logical card；`AutoCardHandler` 查绑定 | `logicalKeys.ownership.test.ts`、`XiuyuanRepository.riff-sync-binding.test.ts`、`CreateCardUseCase.test.ts` |
| Formula attr-tail / rich renderer parity | 安全，公式 attr tail 清理和 rich markdown 渲染在 active renderer | `stripSiyuanBlockAttributeArtifacts()` 被 `MultiClozeCardRenderService` / quick-card adapter / kramdown gateway 复用；`RichMarkdownContent` / `LargeTextEditorDialog` 在 review/AI active surfaces | `MultiClozeCardRenderService.inline-formula.test.ts`、`SiyuanBlockAdapter.test.ts`、`XiuyuanListTemplateCard.test.ts`、AI pane specs |
| AI rich context / dual prompt contract | 安全，Use Context 与行为 prompt / system contract 已分层 | `attachedContexts` 进入 `AIWorkbenchService` request payload；`AIPromptContractRegistry` 持有 code-owned JSON contract；`AIPromptComposer` 保持 behavior prompts 不含 schema 字段 | `AIWorkbenchService.review-session.test.ts`、`AIPromptContractRegistry.test.ts`、`AIPromptComposer.test.ts` |
| Early AI workbench sessions / prompt exposure | 安全，active runtime 已从早期 result-panel / promptProfiles 迁到 tree/chat + skill tabs | `AIWorkbenchSessionStoreService.findLatestByReviewChatKey()`、`AIWorkbenchService.deriveReviewChatKey()`、`AIChatSkillRegistry`、`AIPromptComposer`、`AIPromptContractRegistry` 承接 current path；legacy `make-cards` 被归一/丢弃旧 candidate board | `AIWorkbenchSessionStoreService.test.ts`、`AIWorkbenchService.review-session.test.ts`、`AIChatSkillRegistry.test.ts`、`AIPromptComposer.test.ts`、`settings-normalization.test.ts` |
| AI Daily Note draft / document context | 安全，AI draft 和 document-block body 读取都有 active port/service | `AIDailyNoteDraftService`、`AISiyuanAdapter.createDailyNote()`、`AIWorkbenchService` document body context、`custom-fsrs-ai-*` cleanup policy 都在 active path | `AIDailyNoteDraftService.test.ts`、`AISiyuanAdapter.test.ts`、`AIWorkbenchService.review-session.test.ts`、`BlockAttrCleanupService.test.ts` |
| Review header / actions / open-as shell | 安全，早期 header/title/counter/more-menu/empty-state 修复未见回退 | `UnifiedReviewAdapter` 生产 `queueProgress/header`，`ReviewHeader` / `ReviewActions` / `SkipMenuButton` 承接当前 UI，`ReviewView.openAs` 调 `openStandardReviewDialog()`，counter feedback 走 `showMessage()` | `ReviewHeader.spec.ts`、`ReviewActions.spec.ts`、`SkipMenuButton.spec.ts`、`ReviewView.empty-state.spec.ts`、`ReviewView.open-as-menu.spec.ts`、`DialogManager.review-header-variant.test.ts` |
| Progressive excerpt / Topic-derived item flow | 安全，当前主链支持 storage mode、duplicate/open-existing、review route、hyperspace injection、derived item lineage | `ProgressiveReadingService`、`SelectionExcerptService`、`ConfiguredCaptureStorageService`、`TopicDerivedItemService`、`ReviewView` progressive excerpt handlers、`NeuralRoamQueue.injectExcerptIntoHyperspace()` | `ProgressiveReadingService.test.ts`、`ExcerptRecordService.test.ts`、`ProgressiveExcerptHotkeyHandler.test.ts`、`ReviewView.progressive-excerpt-hyperspace.spec.ts`、`TopicDerivedItemService.test.ts` |
| Neural roam virtual/history/browser handoff | 安全，早期 virtual node、history paging、selected-frame、browser jump 已被 active queue/browser path 承接 | `NeuralRoamQueue`、`ConceptNeuralQueue`、`HyperspaceEngine`、`ConceptQueryEngine`、`SRSBrowser` trace/wake panel、`ReviewView.neural-tab-bridge` 共享 neural contracts | `NeuralRoamQueue.test.ts`、`ConceptQueryEngine.backlinks.test.ts`、`HyperspaceEngine.test.ts`、`ReviewView.neural-tab-bridge.spec.ts`、browser neural trace tests |
| Wake convergence / trace tail | 安全，尾部 2026-03-11 wake 四条的 render split、repeat semantics、semantic root、engine switch carry 在 active queue/browser path 有闭环 | `NeuralRoamQueue.getActivationTrace()`、`setCurrentFocus()`、`setEngineMode(...carryCurrentNode)`；`ConceptNeuralQueue` / `HyperspaceEngine` 生成 synthetic root；`SRSBrowser` lazy convergence details、`repeatHitCount`、`convergenceStatus` | `traceAggregation.test.ts`、`NeuralActivationTracePanel.test.ts`、`NeuralHistoryList.test.ts`、`NeuralRoamQueue.test.ts`、`HyperspaceEngine.test.ts` |
| AI toolchain transparency / decision layer tail | 安全，尾部 AI toolchain / decision layer 的 approval、ask-once、bounded write tool、flashcard runtime 均有 active service/test | `AIChatToolRegistry`、`AIChatToolExecutorService`、`AIChatApprovalService`、`AIFlashcardToolService.createSemanticCdfCards()`、`ApplyBlockDiff`、`study-decision` tool group | `AIChatToolExecutorService.test.ts` 覆盖 ask-once/reject/`ApplyBlockDiff` dry run/`DecideStudyAction`；`AIFlashcardToolService.test.ts` 覆盖 semantic CDF creation |
| Doc-tree / block-menu review scope | 安全，递归 doc scope 和 exact card removal 已有 active entry/query/queue 证据 | `CoreReviewEntryService`、`BlockMenuHandler`、`scopeDocIds`、`DeckDataSource` / `DataSourceUtils.applyDocFilter()`、`OrderedStaticSubsetQueueBase` 精确 cardId 移除 | `CoreReviewEntryService.test.ts`、`BlockMenuHandler.core-review-entry.test.ts`、`SRSBrowser.hierarchy-regression.spec.ts`、`SubsetReviewQueue.preferred-card.test.ts` |
| Review current-item hydration tail | 安全，尾部 `review current-item hydration unification` 已在 session controller / queue strategy active path 中统一 current item 水合与 guarded refresh | `Strategy.hydrateCurrentItem` contract、`UnifiedQueueStrategy.hydrateCurrentItem()`、`maybeAddNextDues()`、`reviewSessionController.hydrateDisplayItem()`、`refreshCurrentItem(expectedCurrentCardId/expectedCurrentBlockId)` | `useReviewSession.spec.ts` 覆盖 restored current item hydration、path-loaded hydration、refresh replace、guarded stale refresh skip |
| Xiuyuan malformed / blank Riff fail-open | 安全，startup/full/incremental sync 遇 malformed/blank Riff records 会跳过并避免 destructive cleanup | `XiuyuanSyncService` 记录 malformed input、跳过 blank content、full sync 有 malformed 时禁用 destructive cleanup；local-owned 优先，native remove 只删 managed records | `XiuyuanSyncService.malformed-riff-input.test.ts`、`XiuyuanSyncService.quick-render-hint.test.ts`、`logicalKeys.ownership.test.ts` |
| Round33 summary debt hints | 安全，文件尾部 Round33 summary 不是 task delta；其中 system cleanup 已按 active code 抽查，未升级出新 P0/P1 | `WeightedWalkEngine`、`HistoryFilter`、`PrioritySequencer` 保持 active；`scripts/check-boundaries.cjs` 保留 UI infra allowlist；`MenuManager` 已切到组合根注入 `ManagerSiyuanPort`，其余 application 默认 adapter constructor debt 仍在 | 作为 D-10/D-52 延期债记录；`rg` 仅把 summary 当 debt hint，不把模板/summary 计入 task delta |

## 4. 真实 deferred debt

| ID | 风险 | 条目 | Deferred debt | 为什么安全延期 |
|---|---|---|---|---|
| D-02 | P3 | AI/SRS Arena foundation | kernel RPC、官方 SM 变体、匿名导出、完整 Arena UI 操作、自动写入策略 | Arena 默认关闭且 advisory-only；官方算法接入需要独立 golden cases |
| D-03 | P3 | Review source refresh | 多复习面开启实时刷新后的真实成本 | 默认关闭，shared ws-main path 有测试 |
| D-04 | P3 | Runtime AI context sync | AI session index 缓存、append-only review log 写优化 | 隐藏面板热路径已切断，剩余是打开 AI 后的 IO 优化 |
| D-05 | P3 | CDF concept-card reuse | 统一 concept-card ensure service | 当前 bug 已由 `reuse-existing` 最小修复覆盖，抽服务会扩大 Xiuyuan use case 面 |
| D-06 | P3 | Formula/prepared review | true queue prefetch、image occlusion prepared rendering、历史 multi-cloze 数据清洗 | 需要 queue contract / 图片尺寸 / 备份迁移，不适合混入 UI soft-failure 修复 |
| D-07 | P3 | Review Protyle flicker | answer Protyle 双缓冲、所有 special renderer 动画设计 | 主正文频闪已收口，答案区和特殊 renderer 是独立 UX pass |
| D-08 | P3 | Review log startup wiring | 测试 stderr、既有 i18n/Sass warning | 不影响启动崩溃根因，也非 runtime correctness |
| D-09 | P3 | Retrieval / Incremental manual queues | manual queue metadata、非调度型临时练习、历史重复卡主动清洗、review-tab snapshot version bump | 当前 active path 有 preview-only/write-schedule 明确策略；迁移/清洗需要独立备份和产品语义 |
| D-10 | P3 | Browser / Review boundary convergence | `MenuManager` 默认 `ManagerSiyuanAdapter` 构造已迁到 `ApplicationContext` 注入；仍剩其他 `new *SiyuanAdapter()` 默认构造、UI infrastructure allowlist、大型 Vue/service 文件拆分、Browser helper 去重 | 边界回流 guard 已存在；剩余是继续组合根迁移和文件瘦身 |
| D-11 | P3 | Browser SQL / data-source actions | SQL 行级 action disabled/hidden predicate、全局多卡 projection helper、`MenuActions` 调试日志清理 | SQL 真实卡结果和菜单 parity 已覆盖；行级协议会改 UI/action contract |
| D-12 | P3 | Neural roam AI context | `ReadBlock` / `ReadBlocks` 行为不改，associated-review 真 flashcard 不默认拉 source virtual node | 当前 bug 是当前虚拟节点上下文；扩大工具语义需要产品规则和 AI focused tests |
| D-13 | P3 | Storage / delete events | 删除链历史创建事件残留、短窗口 `deletionTracker` 生命周期统一 | Durable tombstone / blacklist 已挡住回魂；事件瘦身是架构清理 |
| D-14 | P3 | Shared CDF direct scene | cloze concept-definition、browser/preview/AI pane 共享 scene、历史 `directPath/source` 迁移、happy-dom KaTeX 噪音 | Review active scene 已跑通；跨 surface 和历史数据迁移要独立备份/测试 |
| D-15 | P3 | Review native/dialog UI | mobile/tab header 快捷切换、dialog factory close 协议统一、真实思源拖动/截图验收、native 像素级贴近 | 当前 desktop dialog active blockers 已修；剩余偏 UX/平台验收 |
| D-16 | P3 | Review typography / rich renderer | 所有历史卡型 token 化、shared typography primitive、AI/browser rich-content surface 扩展 | 活跃语义 renderer 已接 shared rich host；全量铺开风险更高 |
| D-17 | P3 | Arena v1 | Arena Manager 研究 UI、heatmap/sample replay/manual weights、future AI assisted rewrite scenario registration | Arena 默认关闭且 advisory-only；v1 kernel 可先收真实数据 |
| D-18 | P3 | Review / Browser visual history | 真实截图验收、native pixel spacing、SkipMenuButton 是否彻底改回原生单按钮、Browser toolbar token 最终规范 | 当前 active UI 有 tests；视觉细节需真机/截图，不是恢复风险 |
| D-19 | P3 | UI shell / F toolbox skin | 抽 `SiyuanMemoFlatList/FlatSection`、逐页减少 scoped CSS、legacy `core/native/session.ts` 迁移到统一 dialog helper | 外围 shell 已统一；抽基础组件会跨多个 UI surface |
| D-20 | P3 | AI settings / workbench | provider 管理器、built-in prompt 拖拽、移动端 drag handle、全 AI 快捷键文案统一、compact token 全面化 | 当前阻塞入口已有管理器和 focused tests；剩余是 UX/设置架构打磨 |
| D-21 | P3 | Custom tabs | `TabManager.registerAll()` 降成 test-only、Browser / Review AI 默认打开位置重设 | 启动恢复已靠 `contextReady` 收口；默认布局是产品决策 |
| D-22 | P3 | Browser missing/doc-tree scope | `__lost__` 查询兼容是否彻底下线、PreviewSiyuanPort、application-layer BrowserOpenState、scoped SQL 语义 | UI 已隐藏 missing-block 入口，doc-tree scope 已有显式 `scopeDocIds` |
| D-23 | P3 | Browser / Card delete ownership | ownership-aware delete command、Riff-only/local FSRS/delete diagnostics toast | Browser route 已统一到 manager；更深删除语义跨 Xiuyuan/Riff/sync |
| D-24 | P3 | Progressive Topic/Item | Topic/Item settings DTO 从 `quickCard` 下正式拆出、selection-to-cloze application service、整篇 Topic mark 批量补齐、历史文档/attr 迁移 | 当前 active path 已稳定；schema/历史迁移需要兼容设计 |
| D-25 | P3 | SiYuan mark/updateBlock integration | 全仓 mark 注释和非活跃解析口径清扫、更多 WYSIWYG -> kramdown 逆向还原、块菜单异步上下文解析 | 3.6.5 active 保存失败已在基础设施边界修正 |
| D-26 | P3 | Native cloze / CDF AI schema | 公式 cloze 独立 render target、CDF `items[].text` 是否升级 `cue/answer` schema、漏箭头 preview warning | 当前 concept-coach 合同保守稳定；结构升级会影响 AI/session/schema |
| D-27 | P3 | Queue scoped actions | `hide-current-in-scope` 从 review/strategy 内部命令升级为显式 queue capability，统一 Item/skip/rotate 语义 | 当前 bug 已由 active strategy path 修掉；契约提升是跨 action 重构 |
| D-28 | P3 | Native Riff sync/delete | 抽共享 normalized transaction classifier；手动/plugin delete 与 native delete 的 outbound Riff side-effect 彻底分流 | native remove stale local card 已修；扩大 classifier 会影响 quick-card、doc-tree、native-riff 三条 listener |
| D-29 | P3 | Review source refresh | Browser surface 源块刷新、hidden queue rehydrate、全局 source-to-card reverse index、默认开启性能验收 | 当前只承诺 current visible card 且默认关；全局 invalidation 会跨 storage/datasource/queue |
| D-30 | P3 | Semantic CDF / concept search | `AIFlashcardToolService` 内联 scanner、CDF anchor pane 组件化、concept library placement policy、export markdown 与 creation markdown 是否完全一致 | active semantic creation 已有 rows/kramdown fallback；抽 scanner/组件和产品格式需独立设计 |
| D-31 | P3 | AI provider transport | 非 OpenAI provider 真 streaming parity、provider manager 更细 capability UI、更多模型 structured-output profile | endpoint/DeepSeek empty-body 已在 adapter 层兜住；provider parity 是大 transport/product 批次 |
| D-32 | P3 | AI self-test creation | 任意深 list grammar、多 answer block、per-queue/per-skill target memory、failed async send draft queue | 当前 native modes 和一问一答 list shape 有 focused tests；扩大内容模型会改 Xiuyuan field mapping |
| D-33 | P3 | Xiuyuan sync ownership | Browser/manual lifecycle matrix、checkpoint retry 真库验收、`CardRepository.save()` logical-key CRUD 边界 | sync 主链两阶段提交已偿还；secondary CRUD 边界需另审 hot path |
| D-34 | P3 | Queue runtime / scheduler | `UnifiedQueueStrategy` 按 queue 拆策略类、显式 queue capability、debug console/diagnostics 面板 | incremental loop 和 FilterGroup active semantics 已稳；拆类/诊断 UI 是架构整理 |
| D-35 | P3 | AI workbench tree/chat | multimodal attachments、`/chat` 文档入口、archive/bulk history、graphical tree page、cross-window live sync | tree-backed session 和 review queue chat 已落地；多窗口/多模态会扩大 persistence 和 conflict 语义 |
| D-36 | P3 | AI user skills | import/export、marketplace delivery、自定义 render component、自定义 write-tool SDK | 当前 declarative skill 已可用；可执行扩展面涉及产品安全边界 |
| D-37 | P3 | AI tool execution | 更多 approved write tools 端到端、assistant continuation 细化、SSE streaming/tool-call progress | 当前审批/工具 scaffold 已存在；全面写入需逐工具 application service contract |
| D-38 | P3 | AI provider diagnostics | provider capability registry、structured inspector、历史 session diagnostic 持久化、更多 vendor payload 状态 | 当前 empty-body/raw/alias 诊断足够定位；泛化会扩设置与 session schema |
| D-39 | P3 | Review tab persistence | back-history/rollback/filter-group transfer 统一到一个 review snapshot contract | special renderer 空白恢复已修；完整 session 恢复需新兼容契约 |
| D-40 | P3 | Review special-render identity | 抽 review-surface target resolver，统一 `cardReferenceBlockId/renderedSourceBlockId/editableSourceBlockId` | locate-source active bug 已修；迁移全部 action 会碰 SRS/neural/AI 语义 |
| D-41 | P3 | Riff / AutoCard reconciliation | 更丰富 semantic transaction classifier、真实重复制卡 trace matrix、local/riff lifecycle diagnostics toast | idempotency/local-owned 主链已有；更深诊断是跨 sync/card CRUD 产品面 |
| D-42 | P3 | Rich renderer editing | image-occlusion editable metadata、special renderer Protyle/WYSIWYG 内嵌编辑、全 renderer attr cleanup 统一 | 当前 raw markdown editor 和 formula cleanup 已覆盖活跃 bug；WYSIWYG 会改生命周期 |
| D-43 | P3 | AI context / prompt contract | context provider 服务化、prompt schema health simulator、expert contract editor、i18n copy sweep | 当前 attached context 与 code-owned JSON contract 已稳定；专家可编辑 schema 风险大 |
| D-44 | P3 | AI legacy sessions / prompt migration | 专用 history 管理弹窗、draft presence、legacy make-card/candidate-board 清理工具、prompt health check | active runtime 已迁到 skill/chat/tree session；剩余是 UX/迁移工具，不影响当前 AI 打开/复用 |
| D-45 | P3 | Progressive excerpt anchoring / storage | 精确 range offset 持久化、旧 workbench/excerpt docs 迁移、source-side highlight replay 稳定化、跨 storage mode 真库验收 | active flow 已可创建/去重/回源/注入 review；精确锚点和历史迁移需要备份策略 |
| D-46 | P3 | AI/Progressive Daily Note convergence | shared Daily Note resolver、stale AI draft archive/cleanup、progressive trace 与 AI draft 命名统一 | AI draft 已用 native `createDailyNote()`；progressive/capture 存储统一会跨两个 product flow |
| D-47 | P3 | Review header / shell parity | 非标准 review session tab restore、notification port、first-frame loading skeleton、cross-surface keyboard focus contract、截图验收 | 当前 header/actions/empty/open-as active bugs 已覆盖；剩余会扩大 review workspace contract |
| D-48 | P3 | Neural roam trace / browser UI | wake full DAG、trace perf telemetry、getActivationTrace call-count integration test、glossary sweep、path/selected-frame screenshot coverage | active queue/browser path 有 unit/focused tests；可视化和性能验收需要独立 UI/perf 批次 |
| D-49 | P3 | Doc-tree review-entry scope policy | 把 doc-tree/right-click/retrieval/browser 的 count/filter rules 抽成显式 review-entry scope policy | 当前入口已用 `scopeDocIds` 和 exact cardId；统一所有入口口径会改变用户可见数量 |
| D-50 | P3 | Riff malformed startup diagnostics | 真实库 malformed/blank Riff trace matrix、checkpoint retry 验收、native/plugin delete side-effect 诊断 toast | fail-open 与 local-owned 保护已在 service/tests；诊断和真库矩阵是运维/产品化工作 |
| D-51 | P3 | Review current-item lifecycle | 把 initial/current/path-loaded/refresh current item 统一成显式 lifecycle command，并同步 review snapshot version | active path 已靠 `hydrateCurrentItem` 与 guarded refresh 防错；抽 command 会碰 review tab restore、queue snapshot、special renderer |
| D-52 | P3 | Round33 residual cleanup | `MenuManager` adapter constructor 已收口；仍剩其他组合根 adapter constructor 迁移、UI infra allowlist 收窄、大型 active 文件拆分、mojibake 文档/注释清理、AI descriptor i18n/happy-dom 噪音、Arena UI scenario registration | 都是真实 deferred cleanup；当前 active runtime 未见由这些 summary 项直接造成恢复/错写/丢数据风险 |

## 5. 下一批审计建议

1. backlog 当前真实 task delta 已全量扫完；A-01/A-12 P2 与 D-01 性能证据债均已关闭或移入安全历史锚点。
2. 下一轮建议继续真实 P3：D-10/D-52 Browser / Review 边界清理，优先迁移 `DialogManager` / `TabManager` / `PracticeQueueManager` / `ReviewScopeCardCreationSyncService` 等剩余默认 Siyuan adapter 构造，再收 UI infra allowlist 和大型 active 文件拆分。
3. 若继续收文档债，优先压缩历史 backlog 的误导性 wording，不回滚 active 语义：manual queue preview-only、Arena Challenger label、missing-block UI hidden、AI declarative skills、progressive `⌥⇧X`。
4. 若用户切到“开始修复”，先选一个 P3 小批次；否则可继续只读对账 `ARCHITECTURE.md` / tests coverage。

## 6. 验证记录

本次 D-01 closure 涉及 dev diagnostics/package/docs；仍不改用户 runtime。验证目标：

- 用 `rg` 交叉检查关键 claims：`algorithm_card_state`、`diagnostics:algorithm-state`、`SourceExistenceCache`、`ReviewCommitUseCase`、`QueueItemUnavailableError`、`SrsArenaConflictDialog`、`reviewSourceBlockRefreshEnabled`、`updateLiveReviewContext`、`duplicatePolicy: 'reuse-existing'`、`manual-early-review`、`sessionExcludedCardIds`、`QueryDataSource`、`SubsetReviewQueue`、`Arena Challenger`、`sourceVirtualNodeId`、`deletedCardDTOs`、`CdfDirectScene`、`registerCurrentReviewDialog`、`refreshVisibleContent`、`toolbar__action--practice`、`missing-block-only`、`SelectionTopicContinuationService`、`TopicDerivedItemService`、`contextReady`、`AIPromptContractRegistry`、`updateBlock()`、`NativeRiffSyncTriggerHandler`、`handleNativeRiffRemove`、`applyRuntimeLogSettings`、`resolveEndpointUrl`、`AISelfTestCardCreationService`、`reviewChatKey`、`avoidOnceBlockId`、`SyncChangeSet`、`AIChatSkillRegistry`、`AIChatToolExecutorService`、`genericStructuredResult`、`expectedCurrentCardId`、`reviewState`、`refreshTabSurface`、`ReviewSiyuanPort`、`AutoCardHandler`、`stripSiyuanBlockAttributeArtifacts`、`attachedContexts`、`promptProfiles`、`AIWorkbenchLegacyView`、`candidate-board`、`PROGRESSIVE_EXCERPT_REQUEST_EVENT`、`progressiveAltXExcerptEnabled`、`Alt+Shift+X`、`AIDailyNoteDraftService`、`createDailyNote`、`CoreReviewEntryService`、`scopeDocIds`、`injectExcerptIntoHyperspace`、`malformed Riff`、`local-owned`、`traceAggregation`、`repeatHitCount`、`convergenceStatus`、`AIChatApprovalService`、`ApplyBlockDiff`、`study-decision`、`hydrateCurrentItem`、`maybeAddNextDues`、`previewBreadcrumbData`、`WeightedWalkEngine`、`HistoryFilter`、`PrioritySequencer`。
- 跑 `git diff --check`。
- 跑 `rg -n "[ \t]+$" docs/FULL_HISTORY_RESUME_AUDIT_REPORT.md` 确认新报告无尾随空白。
- 不跑 `pnpm test:run`；本批因触 `src/diagnostics` / `package.json` 跑 `pnpm build`。

执行结果：关键 `rg` 均能回链到 active code 或本报告；`sessionManualAddIds|pruneCompletedManualCards` 在 `src/` 无命中，支持 A-04 的“旧 hook 已非 active path”判断；`toolbar__action--view-toggle` 只有 class/test 存在而无主高亮样式，支持 A-07；`missing-block-only` 仍有兼容查询但 UI tests 明确不暴露，支持 A-08；第 91-120 条新增核对确认 `NativeRiffSyncTriggerHandler -> handleNativeRiffRemove -> SyncChangeSet`、`reviewSourceBlockRefreshEnabled=false` default-off、`applyRuntimeLogSettings()`、AI endpoint/empty-body fallback、self-test native creation、review `reviewChatKey` tree session、incremental `avoidOnceBlockId` 都有 active code/test 证据；第 121-150 条新增核对确认 declarative skills、tool approvals、generic structured results、guarded review refresh、review tab `reviewState` restore、special-render source identity、Riff/AutoCard idempotency、formula attr cleanup、attached contexts 均有 active code/test 证据；第 151-240 条新增核对确认 `promptProfiles` 仅作 legacy ignore、`AIWorkbenchLegacyView` 只作兼容入口、legacy `candidate-board` 会被 session store 丢弃、active AI skill 为 `general-chat` / `concept-coach`、progressive excerpt 当前默认 `⌥⇧X` 并经 `PROGRESSIVE_EXCERPT_REQUEST_EVENT` 路由、AI Daily Note 使用 `createDailyNote()`、doc-tree scope 走 `CoreReviewEntryService` / `scopeDocIds`、hyperspace excerpt injection 存在、Xiuyuan malformed/blank Riff fail-open 和 `local-owned` 保护有 service/test 证据；第 241-尾部新增核对确认真实 task delta 只剩 wake/progressive/AI/review/doc-tree 十条，模板 heading 不计入；wake trace `traceAggregation` / `repeatHitCount` / `convergenceStatus`、AI approval/tool execution `AIChatApprovalService` / `ApplyBlockDiff` / `study-decision`、review `hydrateCurrentItem` / `maybeAddNextDues`、Round33 residual `previewBreadcrumbData` / `WeightedWalkEngine` / `HistoryFilter` / `PrioritySequencer` 均有 active code/test 或延期债证据；A-01/A-05 闭环新增核对确认 `pnpm diagnostics:algorithm-state -- --db "H:\SiYuanXY\data\storage\petal\siyuan-plugin-siyuanmemo\siyuanmemo.db"` 返回 `clean=true`、`dirty=0`、`orphanStateRows=0`、`afterDirty=0`，`AIWorkbenchService.review-session.test.ts` 34 tests 通过；A-02/A-12 剩余 P2 文档漂移已转入安全历史锚点，报告 P2 归零；D-01 闭环新增核对确认 `pnpm diagnostics:browser-sql-profile -- --db "H:\SiYuanXY\data\storage\petal\siyuan-plugin-siyuanmemo\siyuanmemo.db"` 在真实 `426` 行、内存扩容 `1000/5000` 行全部通过预算，`sourceCandidateUpdate` 5k 最大 `94.608ms`，`BrowserApplicationService.deck-query.test.ts` / `SqlUnifiedStorageRepository.test.ts` / `browserService.block-id-paths.test.ts` / `UnifiedStorageCardUpdateAdapter.test.ts` 加 profiler test 共 22 tests 通过；`pnpm build` 通过，i18n 阻断 0，保留既有硬编码提示与 Sass legacy warning；tracked `git diff --check` 退出 0，仅 LF/CRLF 工作区提示；尾随空白检查无命中。
