# 答案渲染功能测试指南

## 测试目标

验证复习界面的答案渲染功能是否正常工作，包括：
1. 答案内容的正确隐藏/显示
2. 下次复习时间的显示
3. 模板卡片的答案块渲染

## 前置条件

1. 已编译插件：`pnpm run build`
2. 已在思源笔记中安装并启用插件
3. 已创建一些测试卡片（包括普通卡片和模板卡片）

## 测试步骤

### 测试 1：答案内容的隐藏/显示

**目标**：验证答案内容在点击"显示答案"前被正确隐藏，点击后正确显示

**步骤**：
1. 创建一个包含标记（`==答案==`）的卡片
2. 打开复习界面（任意复习模式）
3. 观察卡片内容：
   - ✅ 标记内容应该被隐藏（看不到"答案"文字）
   - ✅ 只能看到问题部分
4. 点击"显示答案"按钮
5. 观察卡片内容：
   - ✅ 标记内容应该显示出来（能看到"答案"文字）
   - ✅ 评分按钮上方应该显示下次复习时间（如"1 分钟"、"5 分钟"等）

**预期结果**：
- 答案内容在点击前被隐藏，点击后显示
- 下次复习时间正确显示在评分按钮上方

### 测试 2：下次复习时间的显示

**目标**：验证每个评分按钮上方显示正确的下次复习时间

**步骤**：
1. 打开复习界面
2. 点击"显示答案"按钮
3. 观察评分按钮：
   - ✅ "重来"按钮上方应该显示时间（如"1 分钟"）
   - ✅ "困难"按钮上方应该显示时间（如"5 分钟"）
   - ✅ "良好"按钮上方应该显示时间（如"10 分钟"）
   - ✅ "简单"按钮上方应该显示时间（如"6 天"）
4. 点击任意评分按钮
5. 观察下一张卡片：
   - ✅ 答案应该重新隐藏
   - ✅ 点击"显示答案"后，时间应该正确显示

**预期结果**：
- 每个评分按钮上方都显示对应的下次复习时间
- 时间格式正确（如"1 分钟"、"5 分钟"、"10 分钟"、"6 天"）

### 测试 3：模板卡片的答案块渲染

**目标**：验证模板卡片的正面/背面逻辑正确

**步骤**：
1. 创建一个模板卡片（双块卡片）：
   - 第一个块：问题
   - 第二个块：答案
2. 打开复习界面
3. 观察卡片内容：
   - ✅ 只显示问题块
   - ✅ 不显示答案块
4. 点击"显示答案"按钮
5. 观察卡片内容：
   - ✅ 显示问题块
   - ✅ 显示答案分隔线（"─── 答案 ───"）
   - ✅ 显示答案块
   - ✅ 评分按钮上方显示下次复习时间

**预期结果**：
- 正面只显示问题块
- 点击"显示答案"后显示问题块+答案块+时间

### 测试 4：所有复习模式

**目标**：验证所有复习模式都能正常工作

**步骤**：
1. 测试提取练习模式
2. 测试刻意练习模式
3. 测试神经漫游模式
4. 测试最终冲刺模式
5. 测试难点攻坚模式

**预期结果**：
- 所有复习模式都能正确隐藏/显示答案
- 所有复习模式都能正确显示下次复习时间

### 测试 5：边界情况

**目标**：验证边界情况的处理

**步骤**：
1. 测试没有 `nextDues` 数据的卡片：
   - ✅ 评分按钮上方应该显示空白（不报错）
2. 测试没有 `answerBlockID` 的模板卡片：
   - ✅ 应该正常显示（不报错）
3. 快速切换卡片：
   - ✅ 答案应该正确重置
   - ✅ 不应该出现闪烁或错误

**预期结果**：
- 边界情况都能正确处理，不报错

## 调试技巧

### 查看控制台日志

打开浏览器控制台（F12），查看日志：

```
[FSRS ReviewContent] applyAnswerVisibility called: { hasHidden: true, showAnswer: false }
[FSRS ReviewContent] Hiding answer, adding hide classes
```

### 检查 CSS 类

在浏览器控制台中，检查 Protyle 元素的 CSS 类：

```javascript
// 查找 Protyle 元素
const protyle = document.querySelector('.protyle');
console.log(protyle.className);

// 应该包含以下类（答案隐藏时）：
// - card__block--hidemark
// - card__block--hideli
// - card__block--hidesb
// - card__block--hideh
```

### 检查下次复习时间数据

在浏览器控制台中，检查 UI 状态：

```javascript
// 查找 Vue 组件实例
const reviewView = document.querySelector('.fsrs-review-v2').__vueParentComponent;
console.log(reviewView.state.value.actions.grades);

// 应该看到类似以下的数据：
// [
//   { label: '重来', value: 1, nextDue: '1 分钟', ... },
//   { label: '困难', value: 2, nextDue: '5 分钟', ... },
//   { label: '良好', value: 3, nextDue: '10 分钟', ... },
//   { label: '简单', value: 4, nextDue: '6 天', ... }
// ]
```

## 常见问题

### 问题 1：答案内容没有被隐藏

**可能原因**：
- CSS 类没有正确应用
- Protyle 实例还未渲染完成

**解决方法**：
- 检查控制台日志，确认 `applyAnswerVisibility()` 被调用
- 检查 Protyle 元素的 CSS 类

### 问题 2：下次复习时间没有显示

**可能原因**：
- `nextDues` 数据缺失
- Adapter 没有正确提取数据

**解决方法**：
- 检查控制台日志，查看 `grades` 数组
- 确认卡片数据中包含 `nextDues` 字段

### 问题 3：模板卡片的答案块没有显示

**可能原因**：
- `answerBlockID` 缺失
- 答案块渲染失败

**解决方法**：
- 检查控制台日志，查看 `answerBlockID`
- 确认模板卡片的 meta 数据正确

## 测试完成标准

所有以下条件都满足时，测试通过：

- ✅ 答案内容在点击前被隐藏，点击后显示
- ✅ 下次复习时间正确显示在评分按钮上方
- ✅ 模板卡片的正面/背面逻辑正确
- ✅ 所有复习模式都能正常工作
- ✅ 边界情况都能正确处理
- ✅ 没有控制台错误或警告

## 报告问题

如果发现问题，请提供以下信息：

1. 问题描述
2. 复现步骤
3. 控制台日志
4. 截图（如果适用）
5. 思源笔记版本
6. 插件版本

---

**测试完成后，请在 `.kiro/specs/answer-rendering/tasks.md` 中标记任务 4.2 为完成。**
