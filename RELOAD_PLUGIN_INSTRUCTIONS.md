# 重新加载插件说明

## 问题
代码已经修复并重新编译，但是浏览器中的插件还在使用旧的代码，导致仍然出现 "Unknown card type" 错误。

## 解决方案

需要重新加载插件以使用新编译的代码。

### 方法 1：重启思源笔记（推荐）

1. 关闭思源笔记
2. 重新打开思源笔记
3. 插件会自动加载新的代码

### 方法 2：禁用并重新启用插件

1. 打开思源笔记的设置
2. 进入"集市" → "已下载" → "插件"
3. 找到 "FSRS" 插件
4. 点击"禁用"
5. 等待几秒
6. 点击"启用"

### 方法 3：刷新浏览器（如果使用浏览器版本）

1. 按 `Ctrl + Shift + R`（Windows/Linux）或 `Cmd + Shift + R`（Mac）
2. 强制刷新页面，清除缓存

## 验证修复

重新加载插件后，打开浏览器渐进学习队列表格视图，应该能看到：

1. ✅ 卡片列表正常加载
2. ✅ 不再出现 "Unknown card type" 错误
3. ✅ 之前失败的 4 张卡片正常显示

## 如果仍然失败

如果重新加载后仍然出现错误，请检查：

1. **编译是否成功**: 查看 `dist/index.js` 文件的修改时间，确认是最新的
2. **插件路径**: 确认插件安装在正确的路径
3. **浏览器缓存**: 清除浏览器缓存后重试

## 技术细节

### 修复内容

1. **IncrementalLearningQueue.ts**: 使用 `CardType.Item` 枚举而不是字符串 `'item'`
2. **StorageManager.ts**: 为 `type` 字段提供默认值 `CardType.Item`
3. **type-guards.ts**: 在 `normalizeToFSRSCard()` 中为 `type` 字段提供默认值

### 编译输出

```
dist/index.js   1,677.01 kB │ gzip: 478.28 kB
✓ built in 9.58s
```

编译成功，新代码已经打包到 `dist/index.js`。

## 预期结果

重新加载后，这 4 张卡片应该能正常显示：
- `20260203222510-lg626ip`
- `20260203222457-raq2sfs`
- `20260205105152-w57h904`
- `20260205110918-j7cej9r`

它们的 `type` 字段将自动设置为 `CardType.Item`（即字符串 `'item'`）。
